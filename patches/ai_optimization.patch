--- a/kernel/sched/fair.c
+++ b/kernel/sched/fair.c
@@ -600,6 +600,11 @@ void schedule(void)
 
 	/* AI-based task scheduling */
+	if (current->prio < AI_OPTIMIZATION_THRESHOLD) {
+		boost_task_efficiency(current);
+	}
+
 	/* Existing scheduling logic continues here */