--- a/drivers/gpu/drm/msm/adreno/adreno_gpu.c
+++ b/drivers/gpu/drm/msm/adreno/adreno_gpu.c
@@ -500,7 +500,12 @@ static int adreno_gpu_init(struct adreno_gpu *adreno_gpu)
     /* Set default GPU frequency */
-    adreno_gpu->default_freq = 500000000; /* 500 MHz */
+    adreno_gpu->default_freq = 650000000; /* Overclock to 650 MHz */
+
+    /* Thermal and power efficiency tweaks */
+    adreno_gpu->min_freq = 300000000; /* Lower idle frequency */
+    adreno_gpu->max_freq = 675000000; /* Boost frequency */
+    adreno_gpu->thermal_limit = 620000000; /* Prevent overheating */
 
     return 0;
